// Placeholder di sviluppo. In produzione l'entrypoint del container SOVRASCRIVE
// questo file con i valori reali (apiUrl/version/date). In dev resta vuoto e
// l'app ricade su import.meta.env.
window.__ENV__ = {};
